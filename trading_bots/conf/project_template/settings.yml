###
# Trading-Bots settings for the {{ project_name }} project.
# Generated by 'bots-admin startproject' using Trading-Bots {{ version }}.
###

tag: {{project_name}}

dry_run: False

installed_bots:

logging:
  bots_root: bots
  filename: bots.log
  level:
    low: debug
    high: info

storage:
  name: json
  filename: store.json

timeout: 120

urls:
